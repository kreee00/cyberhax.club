---
<!-- This file defines the TopLayout component, which serves as a layout wrapper for the top section of pages. -->
<template>
  <header class="top-layout">
    <div class="container">
      <slot />
    </div>
  </header>
</template>

<style>
.top-layout {
  background-color: var(--header-background-color, #f8f9fa);
  padding: 16px;
  border-bottom: 1px solid var(--border-color, #e0e0e0);
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
</style>